# 数采仪对接文档

## 1. 文档概述

### 1.1 文档目的
本文档旨在指导数采仪与智能控制器的对接，包括设备编码读取、标定状态监测和温度数据采集。

### 1.2 适用范围
- 环保监测数采仪
- 支持Modbus RTU协议的采集设备
- 需要读取设备状态和数据的第三方系统

### 1.3 文档版本
- 版本：V1.0
- 日期：2026-04-09

---

## 2. 设备信息

### 2.1 基本参数
| 参数项 | 值 | 说明 |
| :--- | :--- | :--- |
| **设备类型** | 智能控制器 | 专用型数据采集与逻辑控制终端 |
| **通讯协议** | Modbus RTU | 标准Modbus RTU从站协议 |
| **默认设备地址** | 20 (0x14) | 可通过寄存器修改 |
| **默认波特率** | 9600 | 支持2400-115200 |
| **数据位** | 8 | 固定 |
| **停止位** | 1 | 固定 |
| **校验位** | 无 | 支持无/奇/偶校验 |

### 2.2 支持的功能码
| 功能码 | 功能描述 | 适用场景 |
| :---: | :--- | :--- |
| **01** | 读线圈状态 | 读取输出状态（采样泵、标定阀等） |
| **02** | 读离散输入状态 | 读取按钮输入状态 |
| **03** | 读保持寄存器 | 读取设备编码、温度数据等 |
| **04** | 读输入寄存器 | 读取实时数据 |
| **05** | 写单个线圈 | 控制输出（需权限） |
| **06** | 写单个寄存器 | 修改参数（需权限） |
| **10** | 写多个寄存器 | 批量修改参数（需权限） |

---

## 3. 关键数据读取

### 3.1 设备编码读取

#### 3.1.1 功能说明
设备编码为24字节的唯一标识，采用ASCII字符串格式，存储在保持寄存器80-91中。设备编码用于标识设备身份，在环保监测系统中必须准确读取。

#### 3.1.2 寄存器信息
| 寄存器地址 | 数据长度 | 数据格式 | 访问方式 |
| :---: | :---: | :--- | :--- |
| 80-91 | 12个寄存器 | 字节数组 | 功能码03 |

#### 3.1.3 通信示例
**请求报文：**
```
14 03 00 50 00 0C 58 3C
```
- 14：设备地址（20）
- 03：功能码（读保持寄存器）
- 00 50：起始寄存器地址（80）
- 00 0C：寄存器数量（12）
- 58 3C：CRC校验

**响应报文：**
```
14 03 18 36 39 37 30 30 30 30 30 30 30 30 30 31 41 42 43 44 45 46 47 31 32 33 34 XX XX
```
- 14：设备地址
- 03：功能码
- 18：字节数（24字节）
- 36 39...33 34：设备编码数据（示例：6970000000001ABCDEFG1234）
- XX XX：CRC校验

---

### 3.2 状态读取

#### 3.2.1 功能说明
通过读取离散输入状态来获取设备的维护和标定状态。维护状态和标定状态是环保监测中的重要参数，用于标识设备是否处于特殊工作状态。

#### 3.2.2 寄存器信息（基于实际代码）
| 离散输入地址 | 功能描述 | 状态含义 |
| :---: | :--- | :--- |
| 0 | 采样按钮 | 1=按下，0=未按 |
| 1 | 标定按钮 | 1=按下（标定状态），0=未按 |
| 2 | 维护按钮 | 1=按下（维护状态），0=未按 |
| 3 | 原位标定按钮 | 1=按下，0=未按 |

**注：** 离散输入使用功能码02读取，每次可读取1个或多个位状态。

#### 3.2.3 读取标定状态
**请求报文：**
```
14 02 00 01 00 01 98 0A
```
- 14：设备地址
- 02：功能码（读离散输入）
- 00 01：离散输入地址（1-标定按钮）
- 00 01：数量（1个）
- 98 0A：CRC校验

**响应报文：**
```
14 02 01 00 XX XX  # 正常状态
14 02 01 01 XX XX  # 标定状态
```

#### 3.2.4 读取维护状态
**请求报文：**
```
14 02 00 02 00 01 68 0B
```
- 14：设备地址
- 02：功能码（读离散输入）
- 00 02：离散输入地址（2-维护按钮）
- 00 01：数量（1个）
- 68 0B：CRC校验

**响应报文：**
```
14 02 01 00 XX XX  # 正常状态
14 02 01 01 XX XX  # 维护状态
```

---

### 3.3 温度数据读取

#### 3.3.1 功能说明
温度数据包括保温箱温度、保温管温度和伴热管线温度，采用IEEE754 Float32浮点数格式（ABCD字节序）。

#### 3.3.2 寄存器信息
| 寄存器地址 | 参数名称 | 数据类型 | 单位 |
| :---: | :--- | :--- | :--- |
| 0-1 | 保温箱温度 | Float32 | °C |
| 2-3 | 保温管温度 | Float32 | °C |
| 4-5 | 伴热管线温度 | Float32 | °C |

#### 3.3.3 读取保温箱温度
**请求报文：**
```
14 03 00 00 00 02 79 CB
```
- 14：设备地址
- 03：功能码（读保持寄存器）
- 00 00：起始寄存器地址（0）
- 00 02：寄存器数量（2）
- 79 CB：CRC校验

**响应报文：**
```
14 03 04 43 36 00 00 4A 39
```
- 14：设备地址
- 03：功能码
- 04：字节数（4字节）
- 43 36 00 00：温度数据（150.0°C）
- 4A 39：CRC校验

#### 3.3.4 读取保温管温度
**请求报文：**
```
14 03 00 02 00 02 38 0B
```

#### 3.3.5 读取伴热管线温度
**请求报文：**
```
14 03 00 04 00 02 F9 0B
```

---

## 4. 完整通信流程

### 4.1 标准轮询流程
```
开始
  │
  ▼
读取设备编码 ──► 确认设备身份
  │
  ▼
读取温度数据 ──► 采集保温箱、保温管、伴热管线温度
  │
  ▼
读取状态数据 ──► 检查维护状态和标定状态
  │
  ▼
数据存储与上报 ──► 将数据存入数采仪并上报平台
  │
  ▼
等待下一次轮询（建议1-5秒）
  │
  ▼
返回开始
```

### 4.2 通信时序建议
| 操作 | 建议间隔 | 说明 |
| :--- | :--- | :--- |
| 设备编码读取 | 每次连接或每小时 | 设备编码不常变化 |
| 温度数据读取 | 1-5秒 | 实时监测温度变化 |
| 状态读取 | 1-5秒 | 实时监测设备状态 |
| 完整轮询 | 1-5秒 | 建议同时读取所有数据 |

---

## 5. 数据解析指南

### 5.1 设备编码解析
- **数据格式**：24字节ASCII字符串
- **编码规则**：厂商识别代码(7-10位) + 厂商产品类别代码(2-5位) + 校验位(1位) + 产品序列号(11位)
- **示例**：6970000000001ABCDEFG1234
- **解析方法**：直接将字节数组转换为字符串

### 5.2 温度数据解析
- **数据格式**：IEEE754 Float32，ABCD字节序
- **数值范围**：-40.0°C ~ 200.0°C（典型值）
- **精度**：0.1°C
- **异常值**：999.0表示传感器故障或未连接

### 5.3 状态解析
| 状态类型 | 数据源 | 判断条件 |
| :--- | :--- | :--- |
| 维护状态 | 离散输入地址3 | 1=维护中，0=正常 |
| 标定状态 | 离散输入地址1 | 1=标定中，0=正常 |
| 原位标定状态 | 离散输入地址2 | 1=原位标定中，0=正常 |

---

## 6. 注意事项

### 6.1 通讯参数配置
- 确保数采仪与控制器的通讯参数完全一致
- 设备地址、波特率、数据位、停止位、校验位必须匹配
- 修改通讯参数后需断电重启生效

### 6.2 数据读取注意事项
- 读取设备编码时需要完整读取12个寄存器（24字节）
- 温度数据为浮点数，需要正确解析字节序（ABCD）
- 离散输入状态每次读取1个或多个位，注意位对齐

### 6.3 异常处理
- 通讯超时：检查物理连接和通讯参数
- 数据异常：检查寄存器地址和数据格式
- 状态异常：检查设备按钮和继电器输出状态

### 6.4 性能优化
- 建议使用功能码10批量读取多个寄存器，减少通信次数
- 合理设置轮询间隔，避免过于频繁的通信
- 对于不常变化的数据（如设备编码），可减少读取频率

---

## 7. 故障排除

### 7.1 通讯失败
| 故障现象 | 可能原因 | 解决方法 |
| :--- | :--- | :--- |
| 无响应 | 设备地址错误 | 检查设备地址设置 |
| 无响应 | 波特率不匹配 | 检查波特率设置 |
| CRC错误 | 通讯线路干扰 | 检查线路连接，增加屏蔽 |
| 超时 | 设备离线 | 检查设备供电和状态 |

### 7.2 数据异常
| 故障现象 | 可能原因 | 解决方法 |
| :--- | :--- | :--- |
| 温度值为0 | 传感器未连接 | 检查传感器连接 |
| 温度值为999 | 传感器故障 | 更换传感器 |
| 设备编码为空 | 未设置设备编码 | 通过配置工具设置 |
| 状态异常 | 按钮故障 | 检查按钮状态 |

### 7.3 调试建议
1. 使用Modbus调试工具（如Modbus Poll、Modbus Slave）进行测试
2. 先测试单个寄存器读取，确认通讯正常后再测试批量读取
3. 使用串口监听工具查看实际通信报文
4. 对比文档中的示例报文，检查数据格式是否正确

---

## 8. 附录

### 8.1 CRC校验计算
```python
def calc_crc16(data):
    """
    计算Modbus CRC16校验
    data: 字节数组
    return: CRC校验值（2字节）
    """
    crc = 0xFFFF
    for byte in data:
        crc ^= byte
        for _ in range(8):
            if crc & 0x0001:
                crc = (crc >> 1) ^ 0xA001
            else:
                crc >>= 1
    return crc.to_bytes(2, 'little')

# 示例：计算读取设备编码的CRC
cmd = bytes([0x14, 0x03, 0x00, 0x50, 0x00, 0x0C])
crc = calc_crc16(cmd)
print(f"CRC: {crc.hex()}")  # 输出: 583c
```

### 8.2 常用报文速查表
| 功能 | 请求报文 (HEX) | 说明 |
| :--- | :--- | :--- |
| 读设备编码 | `14 03 00 50 00 0C 58 3C` | 读取12个寄存器 |
| 读维护状态 | `14 02 00 03 00 01 68 0B` | 读取离散输入3 |
| 读标定状态 | `14 02 00 01 00 02 98 0A` | 读取离散输入1-2 |
| 读保温箱温度 | `14 03 00 00 00 02 79 CB` | 读取寄存器0-1 |
| 读保温管温度 | `14 03 00 02 00 02 38 0B` | 读取寄存器2-3 |
| 读伴热管线温度 | `14 03 00 04 00 02 F9 0B` | 读取寄存器4-5 |

### 8.3 技术支持
- 文档版本：V1.0
- 更新日期：2026-04-09
- 如有疑问，请联系技术支持
